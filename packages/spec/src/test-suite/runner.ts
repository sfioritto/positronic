import type { ComplianceTestConfig } from './types.js';

export function runComplianceTests(config: ComplianceTestConfig): void {
  // TODO: Implement test runner that will run all compliance tests
  console.log(`Running compliance tests for ${config.backendName} backend`);
}
