{
  "name": "@positronic/dev",
  "version": "0.0.1",
  "description": "Development implementations of Positronic service interfaces",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/types/index.ts",
  "scripts": {
    "tsc": "tsc --project tsconfig.json",
    "swc": "swc src -d dist",
    "build": "npm run tsc && npm run swc",
    "clean": "rm -rf tsconfig.tsbuildinfo dist"
  },
  "dependencies": {
    "@positronic/core": "^0.0.1",
    "@positronic/interfaces": "^0.0.1",
    "zod": "^3.24.1"
  }
}
